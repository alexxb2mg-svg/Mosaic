__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/
build/

# local artifacts (embedding tables, downloaded models, built indexes)
data_externes/
index_*/
_bench_index_*/

# downloaded benchmark data (each dataset keeps its own licence, never
# redistributed here — see bench/alloprof.py and bench/scifact.py)
bench/alloprof/
bench/scifact/

# local ty config (machine-specific; see ci.yml for the CI setup)
ty.toml
